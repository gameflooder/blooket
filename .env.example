# GitHub Configuration for Backend URL Auto-Update
# Create a Personal Access Token at: https://github.com/settings/tokens
# Token needs 'repo' scope for private repos, or 'public_repo' for public repos

GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_OWNER=your-github-username
GITHUB_REPO=blooket-backend-url

# Server Configuration
PORT=4500

# Optional: Path to cloudflared if not in system PATH
# CLOUDFLARED_PATH=C:\cloudflared\cloudflared.exe
